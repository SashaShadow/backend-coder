const options = {
    client: "sqlite3",
    connection: {
        filename: "./DB/mydb.sqlite"
    },
    useNullAsDefault: true,
}

module.exports = { options };

//no quiero hacer el resto, es lo mismo xdd